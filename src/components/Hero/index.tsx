import { StyledHero, StyledTitle, StyledDiv } from './Hero'

const Hero = () => (
  <StyledHero>
    <StyledDiv>
      <StyledTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </StyledTitle>
    </StyledDiv>
  </StyledHero>
)

export default Hero
